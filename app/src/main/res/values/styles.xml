<resources>

    <style name="edit_text" parent="@android:style/Widget.EditText">
        <item name="android:textColor">#808080</item>
        <item name="android:drawablePadding">15dp</item>
        <item name="android:padding">14dp</item>
        <item name="android:inputType">textCapCharacters</item>
        <item name="android:layout_alignParentRight">true</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:background">@drawable/edit_text_design</item>
        <item name="android:textSize">@dimen/font_size</item>
        <item name="android:layout_height">45dip</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Dialog default theme. -->
    <style name="MonthPickerTheme" parent="ThemeOverlay.AppCompat.Dialog.Alert"/>


    <style name="movement_paid">
        <item name="android:layout_width">6dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">#79d279</item>
        <item name="android:layout_marginRight">3dp</item>
        <item name="android:layout_marginBottom">5dp</item>
        <item name="android:layout_marginTop">0dp</item>
    </style>

    <style name="movement_to_win">
        <item name="android:layout_width">6dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">#4d94ff</item>
        <item name="android:layout_marginRight">3dp</item>
        <item name="android:layout_marginBottom">5dp</item>
        <item name="android:layout_marginTop">0dp</item>
    </style>

    <style name="movement_expired">
        <item name="android:layout_width">6dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">#ff3333</item>
        <item name="android:layout_marginRight">3dp</item>
        <item name="android:layout_marginBottom">5dp</item>
        <item name="android:layout_marginTop">0dp</item>
    </style>


    <style name="InputLayout" parent="TextAppearance.AppCompat">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textColorHint">@color/colorMainBackground</item>
        <item name="colorAccent">@color/colorMainBackground</item>
        <item name="colorControlNormal">@color/colorMainBackground</item>
        <item name="colorControlActivated">@color/colorMainBackground</item>
    </style>
</resources>
