<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/finality"
            android:icon="@drawable/ic_menu_send"
            android:title="Finality" />
        <item
            android:id="@+id/credit_card"
            android:icon="@drawable/ic_menu_gallery"
            android:title="Credit Card" />
        <item
            android:id="@+id/bank_account"
            android:icon="@drawable/ic_menu_slideshow"
            android:title="Bank Account" />
    </group>

    <item android:title="Movements">
        <menu>
            <item
                android:id="@+id/movements"
                android:icon="@drawable/ic_menu_share"
                android:title="Movements" />
            <item
                android:id="@+id/invoices"
                android:icon="@drawable/ic_menu_send"
                android:title="Faturas" />
        </menu>
    </item>

</menu>
